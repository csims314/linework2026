# scratch / debris
_to_delete/
tmp_obj_*
*.bundle

# large model variants kept locally but out of the repo. Listed by name
# rather than by pattern, so a future model isn't excluded by accident —
# android-merged2.obj is the one that ships.
Models/Android/android-merged.obj
Models/Android/android-solid.obj

# python (make_speeder.py)
__pycache__/
*.py[cod]

# os / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
